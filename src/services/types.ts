export type InputProducts = {
  id: string
  title: string
  thumbnail:string,
  price:number
  quantity: number
};

export type CartProducts = {
  id: string
  title: string
  price: number,
  thumbnail: string,
  quantity: number,
};

export type InputData = {
  name: string
};
